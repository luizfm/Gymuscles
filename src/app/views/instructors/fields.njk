<div class="box">
    <div>avatar_url</div>
    <div>
        <input type="url" name="avatar_url" value="{{instructor.avatar_url}}" placeholder="http://">
    </div>
</div>

<div class="box">
    <div>Nome</div>
    <div>
        <input type="text" name="name" value="{{instructor.name}}" placeholder="Nome dos instrutor">
    </div>
</div>

<div class="box">
    <div>Data de nascimento</div>
    <div>
        <input type="date" placeholder="01/01/2001" name="birth" value="{{instructor.birth}}">
    </div>
</div>


<div class="box">
    <div>Sexo</div>
    <div>
        <span><input type="radio" name="gender" value='M' {%if instructor.gender == "M" %} checked {% endif %}>Masculino</span>
        <span><input type="radio" name="gender" value='F' {%if instructor.gender == "F" %} checked {% endif %}>Feminino</span>
    </div>
</div>


<div class="box">
    <div>Áreas de atuação</div>
    <div>
        <input type="text" name="services" value="{{instructor.services}}"placeholder="Coloque as áreas separadas por virgula">
    </div>
</div>

<button class="button" type="submit">Salvar</button>